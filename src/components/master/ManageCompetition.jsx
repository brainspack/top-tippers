import { Box, Typography } from "@mui/material";
import React from "react";

const ManageCompetition = (props) => {
  return (
    <Box>
      <Typography variant="h5">Inside Manage Competition</Typography>
    </Box>
  );
};

export default ManageCompetition;
