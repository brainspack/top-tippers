import { Box, Typography } from "@mui/material";
import React from "react";

const ManageSport = (props) => {
  return (
    <Box>
      <Typography variant="h5">Inside Manage Sport</Typography>
    </Box>
  );
};

export default ManageSport;

