import { Box, Typography } from "@mui/material";
import React from "react";

const ManageRound = (props) => {
  return (
    <Box>
      <Typography variant="h5">Inside Manage Round</Typography>
    </Box>
  );
};

export default ManageRound;
