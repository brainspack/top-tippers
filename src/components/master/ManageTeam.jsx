import { Box, Typography } from "@mui/material";
import React from "react";

const ManageTeam = (props) => {
  return (
    <Box>
      <Typography variant="h5">Inside Manage Team</Typography>
    </Box>
  );
};

export default ManageTeam;
