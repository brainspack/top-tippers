import { Box, Typography } from "@mui/material";
import React from "react";

const ManageGame = (props) => {
  return (
    <Box>
      <Typography variant="h5">Inside Manage Game</Typography>
    </Box>
  );
};

export default ManageGame;
