import { Box, styled } from "@mui/material";

export const FormInputWrapper = styled(Box)(() => ({
  marginTop: 1,
  padding: "0 15px 12px",
  height: "auto",
  display: "flex",
  flexDirection: "column",
}));
