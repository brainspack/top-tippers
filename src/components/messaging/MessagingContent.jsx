import { Box, Typography } from "@mui/material";
import React from "react";

const MessagingContent = (props) => {
  return (
    <Box>
      <Typography variant="h4">INSIDE MESSAGING CONTENT</Typography>
    </Box>
  );
};

export default MessagingContent;
